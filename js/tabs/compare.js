// js/tabs/compare.js — Compare tab (placeholder for v2)

export function renderCompare(container, _data) {
  container.innerHTML = `
    <div class="compare-placeholder">
      <div class="placeholder-icon">⊞</div>
      <p>Multi-entity comparison coming in a future release.</p>
      <p class="placeholder-sub">
        Select two or more entities to view side-by-side NAV, flows, and cap structure.
      </p>
    </div>
  `;
}
